<section class="row" id="reports">
	<div class="col-md-12">
		<h1 class="page-header">{{serviceName}}</h1>
		<dl>
			<dt>Status</dt>
			<dl>{{status}}</dl>
			<dt>Last Logged</dt>
			<dl>{{lastLogged}}</dl>
		</dl>
{{#if alerts}}
		<h3>alerts</h3>
		<ul>
		{{#each alerts}}
			<li>{{id}} - {{title}}</li>
		{{/each}}
		</ul>
{{/if}}
		<h3>records</h3>
		<ul>
		{{#each records}}
			<li>{{id}} - {{duration}} - {{responseType}}</li>
		{{/each}}
		</ul>
		
</section>